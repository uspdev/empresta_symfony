{% extends 'base.html.twig' %}

{% block body %}
    <br>
    {{ parent() }}
    <h1>Empréstimo visitante</h1>

    {{ form_start(form) }}
        {{ form_row(form.material,{'label': 'Coódigo'}) }}
        {{ form_row(form.visitante,{'label': 'Visitante'}) }}

        <button type="submit" class="btn btn-primary"> Emprestar </button>
    {{ form_end(form) }}

    <a href="{{ path('visitante_search') }}" id="url-list" class="hidden"></a>
    <a href="{{ path('visitante_get') }}" id ="url-get" class="hidden"></a>
    <a href="{{ path('visitante_new') }}" id ="url-new" class="hidden"></a>

{% endblock %}



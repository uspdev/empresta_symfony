{% extends 'base.html.twig' %}

{% block title %}index{% endblock %}

{% block body %}
    <h1>index</h1>

     {% if is_granted('ROLE_USER') %}
         Usuário logado
         <a href=" {{ path('logout') }}">logout</a>
         <br />
         <br />
         <a href=" {{ path('user_index') }}">Administrar usuários</a>
     {% else %}
         Usuário não logado
         <a href=" {{ path('login') }}">login</a>
     {% endif %}

{% endblock %}
